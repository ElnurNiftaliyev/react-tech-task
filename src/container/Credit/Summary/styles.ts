import { makeStyles } from "@mui/styles";

export default makeStyles({
    textarea: {
        margin: "10px 0",
    },
    disabled: {
        "pointer-events": "none",
    },
    link: {
        textDecoration: "none !important",
    },
});
