import { makeStyles } from "@mui/styles";

export default makeStyles({
    root: {
        margin: 0,
        height: 40,
        width: "100%",
        borderRadius: 7,
        "& input::placeholder": {
            fontSize: "14px",
        },
    },
});
